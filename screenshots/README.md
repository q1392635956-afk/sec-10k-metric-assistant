# Screenshots

Add screenshots of the running app here.

Suggested screenshots to capture:

1. `app_metric_aware.png` — a successful metric-aware query showing all five steps and the result
2. `app_baseline.png` — the same question answered in baseline mode
3. `app_computation_details.png` — the expanded "Computation Details" panel showing the formula and extracted values
4. `app_source_evidence.png` — the expanded "Source Evidence" panel showing retrieved passages
5. `eval_results.png` — a summary of the evaluation results

These images can be embedded in your project presentation or README.
